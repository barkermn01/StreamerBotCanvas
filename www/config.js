const Config  = {
    // modules are render in order of top to bottom
    Modules:[
        "emote"
    ],
    emote:{
        AnimationTime:10,
        Speed:{
            Min:100,
            Max:500
        }
    }
}